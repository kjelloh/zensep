#include "Formatter.hpp"
#include <string> // std::getline
#include <variant>
#include <queue>
#include <sstream>
#include <stack>

namespace zensep {

  Formatter::Result Formatter::format(const std::string& source, const Formatter::Options& options) {
    return source; // No operation for now
  }

}
